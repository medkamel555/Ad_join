---


- name: To permit a user access via SSH and console
  shell: /bin/bash -c "/usr/sbin/realm  permit --all"
  register: ad_status
  no_log: True
  ignore_errors: yes

- name: To permit a Amdin group  suderos access
  shell: /bin/bash -c "realm permit -g 'AWS Delegated Administrators'"
  register: ad_status
  no_log: True
  ignore_errors: yes


